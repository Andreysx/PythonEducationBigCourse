# Функция strip() – это предопределенная библиотечная функция Python.
# Она используется для возврата копии исходной строки путем удаления начальных и конечных пробелов,
# символов, переданных в функцию strip().
#
# Другими словами, это функция строки, которая удаляет символы как с левого, так и
# с правого конца строки, указывая набор символов для функции strip() в качестве аргумента.
# По умолчанию она удаляет пробелы из начальной и конечной строки, если в функцию strip() в Python не передается аргумент.
#
#
# strinput = "  $$$$$  No. 1 Welcome to JAVATPOINT!! No. 1 $$$ "
# # use strip() function to remove the set of characters
# res = strinput.strip ( ' $No. 10 !' ) # store result into res variable
# print ( " Given string is: ", strinput)
# print ( " After removing the set of characters: ", res)
